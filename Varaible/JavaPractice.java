// Practice Questions:
import java.util.*;

public class JavaPractice{
    
    public static void main(String arg[]){
        // 1). Find average of three numbers
        // Scanner scan = new Scanner(System.in);
        // float a = scan.nextFloat();
        // float b = scan.nextFloat();
        // float c = scan.nextFloat();

        // float avg = (a+b+c)/3;
        // System.out.println(avg);



        // 2). Find area of the square;
        // Scanner scan = new Scanner(System.in);
        // float side = scan.nextFloat();
        // float area = side*side;
        // System.out.println(area);


        
        /* 3). Enter cost of 3 items from the user (using float data type) - a pencil, a pen and an eraser. You have to output the total cost of the items back to the user as their bill. Add 18% gst */
        // Scanner scan = new Scanner(System.in);
        // int pencil = scan.nextInt();
        // int pen = scan.nextInt();
        // int eraser = scan.nextInt();

        // float gst_amount = (pencil+pen+eraser)*0.18f;
        // float finalCost = (pencil+pen+eraser) + gst_amount;
        // System.out.println(finalCost);



        /* 4). Did it will trow the Error:
                int $ = 10;
        */
        int $ = 10;
        System.out.println($);
        // So in java we can asso define variable name by using dollar symbul.
    }
}
